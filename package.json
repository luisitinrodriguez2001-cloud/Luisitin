{
  "name": "luisitin-root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs && npm run build:css",
    "build:css": "cd luis-site && npx tailwindcss -i src/styles/globals.css -o ../dist/styles.css --minify"
  },
  "dependencies": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.3.3"
  }
}
